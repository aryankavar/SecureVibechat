// Simulate the searchUsers logic against production database from local script
// Note: Requires GOOGLE_APPLICATION_CREDENTIALS but we don't have it... wait.
// Actually, firebase-admin will automatically use the developer credentials if we run 'firebase emulators:exec' or 'firebase functions:shell'.
